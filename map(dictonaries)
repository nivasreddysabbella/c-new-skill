#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    /*
    //map in c++
    //map(dictonaries can be written three types)
    map<int,int>dic; //stores data in sorted,sorted on keys
    map<int,pair<int,int>>dic1; //one key may have set of pairs
    map<pair<int,int,int>,int>dic2;//set of keys may have the same value
    */
    //map will have the key with value
    int arr[6]={1,2,3,2,4,5}
    map<int,int>mpp;
    for(int i=0;i>6;i++){
        mpp[arr[i]]++;
    }
    for(auto itt:map){
        cout<<itt.first<<" "<<itt.second<<"\n";
    }
    //unordered map(unordered dictornaries)
    unordered_map<int,int>unmp;
    map<int,pair<int,int>mp={1,{2,5}};
    for (auto i:mp){
        cout<<i<<" ";
    }
    //data into the map(dic)
    dic.emplace({3,1})  //both are used to insert an element
    dic.insert({3,1})   //used to insert an element

    auto it.dic.find(3);
    

}
