#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    //for declaring the vectors
    vector<int>ds;    //it will create a vector ds
    //vector<int>ds(n,0); //it will create a vector ds and elements are zeros
    //loops in vectors
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        ds.push_back(x);//in python same like as append
    }
    /*
    //loops based on index
    for (int i=0;i<ds.size();i++){
        cout<<ds[i]<<" ";
    }
    input
    5
    1 2 3 4 5
    output 0 0 0 0 0 1 2 3 4 5 
    // loops based on iterators or address//not working

    for (vector<int>it=ds.begin();it!=ds.end();it++)
    {
        cout<<it;
    }
    */
    for (auto i:ds){    //like for i in list --in python
        cout<<i<<" ";
    }
}
